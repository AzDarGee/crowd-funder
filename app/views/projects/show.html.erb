<p><%= link_to "Back to projects", :back %></p>
<h1><%= @project.title %></h1>
<section>
  <%= @project.description.html_safe %>
</section>
<%= image_tag @project.image.url(:medium) %>

<h2>Comments</h2>
<%= render "comments/comments" %>
<h2>New Comment</h2>
<%= render "comments/form" %>